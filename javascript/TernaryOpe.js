function a() {
    let a1 = i === 500 ? 'Correct' : 'Incorrect';
    console.log(a1);
}

let i = 500;

a();

let isLoggedIn = true;
let msg = isLoggedIn ? "Welcome User" : "Login Please";
console.log(msg);

const hello = () => {
    let isLoggedIn = true;
    let msg = isLoggedIn ? "Welcome User" : "Login Please";
    console.log(msg);
}

hello();

